# HotWordsCount configuration (UTF-8)
# Lines starting with '#' are comments.
# Key-value format: key = value

# Input file containing UTF-8 Chinese sentences (relative to project root)
input_file = test_sentences.txt

# Output file path (relative to project root or absolute)
output_file = output.txt

# Dictionary directory (relative to project root or absolute)
# Contains: jieba.dict.utf8, hmm_model.utf8, user.dict.utf8, idf.utf8, stop_words.utf8
dict_dir = dict


#the mode to use jieba
mode = tagres

# Top-K keywords to extract (not used in minimal main yet)
topk = 10

#Time range to count the hot words
time_range = 10

#choose work_type: 1 = file input, 2 = handinput
work_type = 2
